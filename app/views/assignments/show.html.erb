<body class="assign-show"></body>

<p id="notice"><%= notice %></p>

<p>
  <strong>Ship:</strong>
  <%= @assignment.ship.name %>
</p>

<p>
  <strong>Job:</strong>
  <%= @assignment.job.name %>
</p>

<p>
  <strong>Ship Owner/Captain:</strong>
  <%= @assignment.ship.user.email %>
</p>

<% if current_user && current_user.id == @assignment.ship.user_id %>
<%= link_to 'Edit', edit_assignment_path(@assignment) %> |
<% end %>
<%= link_to 'Back', assignments_path %>


